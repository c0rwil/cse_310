//
// Created by metzi on 9/17/22.
//
/*TODO this needs to be finished*/
#include "IS.h"
int *A,p,n;

void insertion_sort(A[],n,p){S
    for i := 2 to n do key := A[i]
            //Insert A[i] into sorted
            j:=i-1
                    while j > 0 and A[j] > key do A[j+1] := A[j]
                            j := j-1
                                    A[j+1] := key
};
