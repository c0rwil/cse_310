//
// Created by metzi on 9/17/22.
//
/*TODO this needs to be finished*/
#include "IS.h"
int *A,p,n,j;

void insertion_sort(int A[],int n, int p){
    for (int i = 1; i <= n; i++)
        do key : A[i]; j:i-1;
                while (j > 0 and A[j] > key) do A[j+1] : A[j]:= j-1A[j+1] := key
};